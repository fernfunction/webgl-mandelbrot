{
  "name": "webgl-mandelbrot",
  "version": "1.0.0",
  "description": "Interactive infinite Mandelbrot zoom sandbox (WebGL2), compiled from TypeScript into a single self-contained HTML file.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@iconify-json/circle-flags": "^1.2.10",
    "@iconify-json/mdi": "^1.2.3",
    "typescript": "^5.6.3",
    "unplugin-icons": "^23.0.1",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
